[pytest]
testpaths = tests
pythonpath = .
norecursedirs = integration .git .venv __pycache__
markers =
    live: проверка реального запущенного API (см. tests/integration)
filterwarnings =
    ignore:datetime\.datetime\.utcnow\(\) is deprecated:DeprecationWarning
